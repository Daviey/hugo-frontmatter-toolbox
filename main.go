// Package main is the entry point for the hugo-frontmatter-toolbox application.
package main

import "github.com/Daviey/hugo-frontmatter-toolbox/cmd"

// main executes the root command.
func main() {
	cmd.Execute()
}
